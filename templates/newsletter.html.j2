<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>{{ title }}</title>
  <style>
    body { margin:0; padding:0; background:#f5f7fb; }
    img { display:block; max-width:100%; height:auto; }
    a { color:#0ea5e9; text-decoration:none; }
    .wrapper { width:100%; background:#f5f7fb; padding:24px 0; }
    .main { background:#ffffff; margin:0 auto; width:100%; max-width:760px; border-radius:16px; overflow:hidden; box-shadow:0 4px 20px rgba(0,0,0,0.06); font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,"Noto Sans",sans-serif; color:#0b1324; }
    .header { background:#0f172a; color:#fff; position:relative; }
    .hero { background:url('{{ hero_image_url or "https://images.unsplash.com/photo-1542838132-92c53300491e?q=80&w=1600&auto=format&fit=crop" }}') center/cover no-repeat; }
    .overlay { background:rgba(2,6,23,0.6); }
    .inner { padding:36px 28px; }
    .pill { display:inline-block; padding:6px 12px; border-radius:999px; background:#10b981; color:#052e2b; font-weight:800; letter-spacing:.3px; text-transform:uppercase; font-size:12px; }
    h1 { margin:10px 0 0; font-size:30px; color:#fff; }
    .sub { margin:8px 0 0; color:#d1d5db; font-size:14px; }
    .block { padding:26px 28px; border-top:1px solid #eef2f7; }
    .kicker { margin:0 0 6px; text-transform:uppercase; font-size:12px; color:#64748b; letter-spacing:.5px; }
    .title { margin:0 0 14px; font-size:20px; }
    .split { width:100%; border-collapse:collapse; }
    .split td { vertical-align:top; }
    .card { background:#f9fafb; border:1px solid #eef2f7; border-radius:12px; overflow:hidden; }
    .pad { padding:16px; }
    .meta { font-size:13px; color:#374151; }
    .jp { font-family:"Noto Sans JP","Hiragino Kaku Gothic ProN","Yu Gothic",Meiryo,system-ui,sans-serif; }
    .cta { text-align:center; padding:18px; }
    .btn { background:#0ea5e9; color:#fff; padding:12px 18px; border-radius:10px; font-weight:700; display:inline-block; }
    .btn-outline { border:2px solid #0ea5e9; color:#0ea5e9; padding:10px 16px; border-radius:10px; font-weight:700; display:inline-block; }
    .footer { color:#64748b; font-size:12px; text-align:center; padding:24px; }
    @media (max-width:640px){ .inner,.block{ padding:22px 18px; } }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="main">
      <div class="header hero">
        <div class="overlay">
          <div class="inner">
            <span class="pill">{{ pill_text or "Newsletter" }}</span>
            <h1>{{ heading or "Art de Vivre Durable – FR・JP" }}</h1>
            <p class="sub">{{ subheading or "Gastronomie・Artisanat・Design・Voyages curated" }}</p>
          </div>
        </div>
      </div>

      <div class="block">
        <p class="kicker">Édito / はじめに</p>
        <p>{{ intro_fr or "Bienvenue dans ce numéro bilingue qui relie France et Japon via gastronomie, artisanat et design durable. Version FR et JP, avec adresses, prix, cartes et liens." }}</p>
        <p class="jp">{{ intro_jp or "ガストロノミー、工芸、サステナブル・デザインを軸に日仏を結ぶバイリンガル版。住所・価格・地図・リンクを分かりやすく掲載します。" }}</p>
        {% if period %}<p class="meta">Période : {{ period }}</p>{% endif %}
      </div>

      {% for sec in sections %}
      <div class="block">
        <p class="kicker">{{ sec.kicker or sec.category or "" }}</p>
        <h3 class="title">{{ sec.heading }}</h3>
        <table class="split" width="100%">
          <tr>
            <td width="50%" style="padding-right:10px;">
              <div class="card">
                {% if sec.image_left %}
                  <img src="{{ sec.image_left }}" alt="">
                {% endif %}
                <div class="pad">
                  <p><strong>FR :</strong> {{ sec.fr | safe }}</p>
                  {% if sec.meta_fr %}<p class="meta">{{ sec.meta_fr | safe }}</p>{% endif %}
                </div>
              </div>
            </td>
            <td width="50%" style="padding-left:10px;">
              <div class="card">
                {% if sec.image_right %}
                  <img src="{{ sec.image_right }}" alt="">
                {% endif %}
                <div class="pad jp">
                  <p><strong>JP：</strong> {{ sec.jp | safe }}</p>
                  {% if sec.meta_jp %}<p class="meta">{{ sec.meta_jp | safe }}</p>{% endif %}
                </div>
              </div>
            </td>
          </tr>
        </table>

        {% if sec.places %}
          <div style="margin-top:10px;">
            <div class="card">
              <div class="pad">
                <ul style="margin:0; padding-left:18px;">
                  {% for p in sec.places %}
                    <li><strong>{{ p.name }}</strong>{% if p.city %} — {{ p.city }}{% endif %}{% if p.address %}, {{ p.address }}{% endif %}{% if p.price %} • {{ p.price }}{% endif %}{% if p.map_url %} • <a href="{{ p.map_url }}" target="_blank">Map</a>{% endif %}</li>
                  {% endfor %}
                </ul>
              </div>
            </div>
          </div>
        {% endif %}

        {% if sec.sources %}
          <p class="meta">Sources :
            {% for s in sec.sources %}
              <a href="{{ s.url }}" target="_blank">{{ s.title }}</a>{% if not loop.last %} · {% endif %}
            {% endfor %}
          </p>
        {% endif %}
      </div>
      {% endfor %}

      {% if mymaps_embed_url %}
      <div class="block">
        <p class="kicker">Carte / 地図</p>
        <h3 class="title">Carte interactive des adresses</h3>
        <div class="card">
          <div class="pad">
            <div style="position:relative;padding-top:56.25%;border-radius:12px;overflow:hidden;border:1px solid #e5e7eb;">
              <iframe title="My Maps FR–JP" src="{{ mymaps_embed_url }}" style="position:absolute;left:0;top:0;width:100%;height:100%;border:0;"></iframe>
            </div>
            <p class="meta">Astuce : importez/actualisez votre CSV dans My Maps pour une mise à jour rapide.</p>
          </div>
        </div>
      </div>
      {% endif %}

      <div class="block">
        <p class="kicker">Abonnement / 購読</p>
        <div class="cta">
          <a class="btn" href="{{ cta_subscribe_url or 'subscribe_landing.html' }}" target="_blank">S’abonner (Stripe) / 購読する</a>
          <span style="display:inline-block;width:12px;"></span>
          <a class="btn-outline" href="{{ legal_url or 'mentions_legales_cgv_fr.html' }}" target="_blank">Mentions légales & CGV</a>
        </div>
      </div>

      <div class="footer"> © {{ (now.year if now is defined) else "" }} Art de Vivre Durable FR–JP — Template Jinja2 compatible Ghost.
    </div>
    </div>
  </div>
</body>
</html>
